//제곱수 판별하기
class Solution {
    public int solution(int n) {
        int answer =   0; 
        if(String.valueOf((double)Math.sqrt(n)).contains(".0")){
            answer = 1;
        }else{
            answer = 2;
        }
        return answer;
    }
 
//피자 나눠 먹기
class Solution {
    public int solution(int slice, int n) {
        int answer = 0;
        if(n > slice){
            if(n % slice == 0){
                answer = n / slice;
            }else{
                answer = (n / slice) +1;
            }
        }
        else{
            answer = 1;
        }
        return answer;
    }
}

//최댓값 만들기
정수 배열 numbers가 매개변수로 주어집니다.
 numbers의 원소 중 두 개를 곱해 만들 수 있는 최댓값을 
return하도록 solution 함수를 완성해주세요.

